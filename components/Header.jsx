import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Header = () => {
  return (
    <View>
      <Text style={{ color: "black" }}>Header1</Text>
    </View>
  );
};

export default Header;

const styles = StyleSheet.create({});
